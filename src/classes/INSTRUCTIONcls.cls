public with sharing class INSTRUCTIONcls 
{
    public String domainName = '';
    public INSTRUCTIONcls()
    {
        //domainName = String.valueOf(System.Url.getHost());   
        System.debug(domainName);
    }
     
}